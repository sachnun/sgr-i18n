name: ci
on:
  push:
  pull_request:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - run: uv sync --extra dev
      - run: uv run pytest -q
      - run: uv run ruff check tools tests
      - run: uv run ruff format --check tools tests
      - run: uv run python -m sgre validate --translations-dir tests/fixtures/valid
      - run: uv run python -m sgre validate
      - run: uv run python -m sgre --help
